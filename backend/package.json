{
  "name": "username-checker-backend",
  "version": "0.1.0",
  "description": "Backend to check if a username is unavailable",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js"
  }, 
  "author": "Snehil Saluja",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
